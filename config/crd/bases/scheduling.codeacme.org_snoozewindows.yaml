---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: snoozewindows.scheduling.codeacme.org
spec:
  group: scheduling.codeacme.org
  names:
    kind: SnoozeWindow
    listKind: SnoozeWindowList
    plural: snoozewindows
    singular: snoozewindow
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SnoozeWindow is the Schema for the snoozewindows API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SnoozeWindowSpec defines the desired state of SnoozeWindow.
            properties:
              endTime:
                type: string
              selector:
                type: string
              startTime:
                type: string
              timeZone:
                type: string
            required:
            - endTime
            - startTime
            - timeZone
            type: object
          status:
            description: SnoozeWindowStatus defines the observed state of SnoozeWindow.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
